pipeline {
    agent any

    stages {
        stage('build') {
            docker{
                image 'node:18-alpine'
        
            }
            steps {
                sh'''
                echo 'Hello World';
                ls -la
                node --version
                npm  --version
                '''
            }
        }
    }
}
